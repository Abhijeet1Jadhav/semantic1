name: Deployment

concurrency: Development

on:
  #workflow_run:
   # workflows: [PR Workflow]
    #types: 
     # - completed
  pull_request:
    types:
      - closed
jobs:
  Dev_Deployment:
    runs-on: ubuntu-latest
    environment: Development
    steps:
      - name: deploy
        run: echo "hello dev"
